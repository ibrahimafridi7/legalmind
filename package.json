{
  "name": "legalmind-ai-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm --workspace frontend run dev",
    "build": "npm --workspace frontend run build",
    "test": "npm --workspace frontend test"
  },
  "dependencies": {
    "util-deprecate": "^1.0.2"
  }
}
